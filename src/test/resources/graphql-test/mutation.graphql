mutation ($graphqlObject: GraphqlObjectInput!){
    publishGraphqlObject (
        graphqlObjectInput: $graphqlObject
    ) {
        chanel
        entries {
            key
            value
        }
    }
}